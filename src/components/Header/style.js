import {
    StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
    header: {
      flexDirection: 'row',
      alignItems:'center',
      height: 60,
      backgroundColor: 'rgb(114, 34, 38)',
    },

    logoutBtn: {
        position: 'absolute',
        right: 20,
    }
  
  });

  export default styles;